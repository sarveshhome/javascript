import str from './modules/Search';  
//import {add,multiply, ID} from './views/searchView';
//import {add as a,multiply as m, ID} from './views/searchView';
import * as SearchView from './views/searchView';
//console.log(`Using import function ${add(ID,2)} and ${multiply(3,7)} and ${str} `);
//console.log(`Using import function ${a(ID,2)} and ${m(3,7)} and ${str} `);
console.log(`Using import function ${SearchView.add(ID,2)} and ${SearchView.multiply(3,7)} and ${str} `);

